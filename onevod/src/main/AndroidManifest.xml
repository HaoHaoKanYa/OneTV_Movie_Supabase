<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    
    <!-- 网络权限 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    
    <!-- 存储权限 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    
    <!-- 其他权限 -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    
    <application>
        <!-- 导出的Activity供主应用调用 -->
        <activity
            android:name="com.fongmi.android.tv.ui.activity.HomeActivity"
            android:exported="true"
            android:theme="@style/AppTheme"
            android:launchMode="singleTop" />
            
        <activity
            android:name="com.fongmi.android.tv.ui.activity.VodActivity"
            android:exported="true"
            android:theme="@style/AppTheme" />
            
        <activity
            android:name="com.fongmi.android.tv.ui.activity.VideoActivity"
            android:exported="true"
            android:theme="@style/AppTheme"
            android:configChanges="orientation|screenSize|keyboardHidden" />
            
        <!-- 服务 -->
        <service
            android:name="com.fongmi.android.tv.service.PlaybackService"
            android:exported="false" />
            
        <!-- 接收器 -->
        <receiver
            android:name="com.fongmi.android.tv.receiver.ActionReceiver"
            android:exported="false" />
    </application>
</manifest>
